/*var mithril = require('mithril'),
    home = require('./home'),
    user = require('./user'),
    map = require('./map'),
    suggest = require('./suggest');

m.route(document.body, {
  '/' : home,
  '/search' : search,
  '/user' :  user,
  '/map' : map,
  '/suggest' : suggest
});


*/
